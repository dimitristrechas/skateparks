web: rm -f tmp/pids/server.pid && bundle exec rake db:migrate && env RUBY_DEBUG_OPEN=true bin/rails server -b 0.0.0.0 -p ${PORT:-3002}
css: bin/rails tailwindcss:watch[poll]
worker: bundle exec sidekiq -e $RAILS_ENV
