# Installation

1. `bundle install`
2. ` VISUAL="code --wait" bin/rails credentials:edit --environment=production`
3. Copy .env.example to .env and fill variables
4. `docker-compose build`
5. `docker-compose up`
