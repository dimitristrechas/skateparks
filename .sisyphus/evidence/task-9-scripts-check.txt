Task 9: scripts.sh updated for Minitest
========================================

Changes made:
1. Line 67: "Run RSpec tests" → "Run tests"
2. Line 176: "Running RSpec tests with coverage..." → "Running tests with coverage..."
3. Line 177: "bundle exec rspec --format progress" → "bin/rails test"

Verification:
- grep -i "rspec" scripts.sh: REPLACED (no matches)
- grep -q "bin/rails test" scripts.sh: NEW_COMMAND_FOUND

All RSpec references removed. Minitest commands in place.
