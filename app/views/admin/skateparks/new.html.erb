<h1 class="mb-8 text-3xl font-bold">Create a skatepark</h1>
<%= render "form", skatepark: @skatepark %>

<div class="flex">
  <%= render(
    LinkComponent.new(
      title: "Back to all skateparks",
      url: admin_skateparks_path,
    ),
  ) %>
</div>
