<%= render(TitleComponent.new(title: "Edit skatepark: #{@skatepark.name}")) %>

<%= render "form", skatepark: @skatepark %>

<div class="flex">
  <%= render(
    LinkComponent.new(
      title: "Back to skateparks",
      url: admin_skateparks_path
    )
  ) %>
</div>

