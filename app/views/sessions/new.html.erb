<div class="mx-auto w-full md:w-2/3">
  <%= render "shared/flash" %>

  <h1 class="text-4xl font-bold"><%= t('sessions.title') %></h1>

  <%= form_with url: session_url, class: "contents" do |form| %>
    <div class="my-5">
      <%= form.label :email_address, t('sessions.email_label'), class: "block font-medium" %>
      <%= form.email_field :email_address, required: true, autofocus: true, autocomplete: "username", placeholder: t('sessions.email_placeholder'), value: params[:email_address], class: "block shadow-sm rounded-md border border-gray-400 focus:outline focus:outline-2 focus:outline-blue-600 px-3 py-2 mt-2 w-full text-gray-900 bg-white dark:text-white dark:bg-gray-700 dark:border-gray-600" %>
    </div>

    <div class="my-5">
      <%= form.label :password, t('sessions.password_label'), class: "block font-medium" %>
      <%= form.password_field :password, required: true, autocomplete: "current-password", placeholder: t('sessions.password_placeholder'), maxlength: 72, class: "block shadow-sm rounded-md border border-gray-400 focus:outline focus:outline-2 focus:outline-blue-600 px-3 py-2 mt-2 w-full text-gray-900 bg-white dark:text-white dark:bg-gray-700 dark:border-gray-600" %>
    </div>

    <div class="col-span-6 sm:flex sm:items-center sm:gap-4">
      <div class="inline">
        <%= form.submit t('sessions.submit'), class: "w-full sm:w-auto text-center rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
      </div>

      <div class="mt-4 text-sm text-gray-500 sm:mt-0">
        <%= link_to t('sessions.forgot_password'), new_password_reset_path, class: "text-gray-700 underline hover:no-underline" %>
      </div>
    </div>
  <% end %>
</div>
